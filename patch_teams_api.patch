--- internal/graph/teams_api.go
+++ internal/graph/teams_api.go
@@ -395,37 +395,30 @@
-func (c *Client) GetChannelSubstrateID(channelThreadID string) (string, error) {
-	url := "https://teams.microsoft.com/api/mt/part/amer-02/beta/users/channels/" + channelThreadID
-	req, err := http.NewRequest("GET", url, nil)
-	if err != nil {
-		return "", err
-	}
-	req.Header.Set("Authorization", "Bearer "+c.SpacesToken)
-	resp, err := c.HTTPClient.Do(req)
-	if err != nil {
-		return "", err
-	}
-	defer resp.Body.Close()
-	body, err := io.ReadAll(resp.Body)
-	if err != nil {
-		return "", err
-	}
-	var res struct {
-		SubstrateGroupId string `json:"substrateGroupId"`
-	}
-	if err := json.Unmarshal(body, &res); err == nil && res.SubstrateGroupId != "" {
-		return res.SubstrateGroupId, nil
-	}
-	if matches := regexp.MustCompile(`"substrateGroupId":"([^"]+)"`).FindStringSubmatch(string(body)); len(matches) > 1 {
-		id := matches[1]
-		return id, nil
-	}
-	return "", fmt.Errorf("substrateGroupId not found")
-}
-
-func (c *Client) AddChannelMember(teamGUID, channelThreadID, userID, tenantID string) error {
-	substrateID, err := c.GetChannelSubstrateID(channelThreadID)
-	if err != nil {
-		return fmt.Errorf("could not get channel substrate ID: %w", err)
-	}
-
-	channelOID := fmt.Sprintf("OID:%s@%s", substrateID, tenantID)
-	userOID := fmt.Sprintf("OID:%s@%s", userID, tenantID)
-	payload := fmt.Sprintf(`{"users":[{"id":"%s","role":1}]}`, userOID)
-	url := fmt.Sprintf("https://teams.microsoft.com/fabric/amer/templates/api/teams/%s/channels/%s/users?forceSync=false",
-		substrateID, channelOID)
-	req, err := http.NewRequest("POST", url, strings.NewReader(payload))
+func (c *Client) AddChannelMember(teamGUID, channelThreadID, userID, tenantID string) error {
+	payload := fmt.Sprintf(`{
+		"@odata.type": "#microsoft.graph.aadUserConversationMember",
+		"roles": [],
+		"user@odata.bind": "https://graph.microsoft.com/v1.0/users('%s')"
+	}`, userID)
+
+	endpoint := fmt.Sprintf("/teams/%s/channels/%s/members", teamGUID, channelThreadID)
+	req, err := http.NewRequest("POST", baseURL+endpoint, strings.NewReader(payload))
 	if err != nil {
 		return err
 	}
-	req.Header.Set("Authorization", "Bearer "+c.FabricToken)
+	req.Header.Set("Authorization", "Bearer "+c.GraphToken)
 	req.Header.Set("Content-Type", "application/json")
+
 	resp, err := c.HTTPClient.Do(req)
 	if err != nil {
 		return err
 	}
 	defer resp.Body.Close()
 	if resp.StatusCode == 200 || resp.StatusCode == 201 || resp.StatusCode == 202 {
 		return nil
 	}
 	body, _ := io.ReadAll(resp.Body)
 	return fmt.Errorf("add channel member error %d: %s", resp.StatusCode, string(body))
 }
 
@@ -467,23 +460,37 @@
 
 func (c *Client) RemoveChannelMember(teamGUID, channelThreadID, userID, tenantID string) error {
-	substrateID, err := c.GetChannelSubstrateID(channelThreadID)
+	membersEndpoint := fmt.Sprintf("/teams/%s/channels/%s/members", teamGUID, channelThreadID)
+	body, err := c.doReq(membersEndpoint)
 	if err != nil {
-		return fmt.Errorf("could not get channel substrate ID: %w", err)
+		return err
 	}
-	channelOID := fmt.Sprintf("OID:%s@%s", substrateID, tenantID)
-	userOID := fmt.Sprintf("OID:%s@%s", userID, tenantID)
-	url := fmt.Sprintf("https://teams.microsoft.com/fabric/amer/templates/api/teams/%s/channels/%s/users/%s?forceSync=false",
-		substrateID, channelOID, userOID)
-	req, err := http.NewRequest("DELETE", url, nil)
+
+	var res struct {
+		Value []struct {
+			ID     string `json:"id"`
+			UserID string `json:"userId"`
+		} `json:"value"`
+	}
+	if err := json.Unmarshal(body, &res); err != nil {
+		return err
+	}
+
+	membershipID := ""
+	for _, m := range res.Value {
+		if m.UserID == userID {
+			membershipID = m.ID
+			break
+		}
+	}
+	if membershipID == "" {
+		return fmt.Errorf("member not found in channel")
+	}
+
+	endpoint := fmt.Sprintf("/teams/%s/channels/%s/members/%s", teamGUID, channelThreadID, membershipID)
+	req, err := http.NewRequest("DELETE", baseURL+endpoint, nil)
 	if err != nil {
 		return err
 	}
-	req.Header.Set("Authorization", "Bearer "+c.FabricToken)
+	req.Header.Set("Authorization", "Bearer "+c.GraphToken)
 	resp, err := c.HTTPClient.Do(req)
 	if err != nil {
 		return err
 	}
 	defer resp.Body.Close()
 	if resp.StatusCode == 200 || resp.StatusCode == 204 {
 		return nil
 	}
-	body, _ := io.ReadAll(resp.Body)
-	return fmt.Errorf("remove channel member error %d: %s", resp.StatusCode, string(body))
+	body2, _ := io.ReadAll(resp.Body)
+	return fmt.Errorf("remove channel member error %d: %s", resp.StatusCode, string(body2))
 }
